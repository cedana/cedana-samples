# Build artifacts produced by build.sh (compiled samples + upstream test config
# fetched at build time). Only the vendored harness/gate and the wrapper scripts
# are tracked here: build.sh, run-tests.sh, run_tests.py, compare-results.py.
bin/
test_args.json

# Local smoke-run output (run_tests.py writes results here when run in-place).
results.json
APM_*.txt
